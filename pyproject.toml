[project]
name = "30-days-of-python-by-friso"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fmeeusen", email = "friso@meeusen.eu" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
30-days-of-python-by-friso = "30_days_of_python_by_friso:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
